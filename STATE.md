# STATE — live snapshot (overwritten every session, NOT locked)

This file is a snapshot of *right now*, not a history. It is overwritten wholesale each time
someone updates it — never appended to. For history, see `worklog.md`. **Tie-break rule: if
this file and `worklog.md` disagree about what is currently true, this file wins; the worklog
explains how we got here.**

Last updated: 2026-08-27 05:51 CEST — by Mulaydm10 (initial scaffold, no work started yet)

## Deadline

Deadline: TODO(Mulaydm10) — see `COMPETITION.md` (single source of truth for event facts;
do not restate the date/time/timezone here, only link to it).
Time remaining: TODO(Mulaydm10) — cannot compute until the deadline above is filled in.

## Done

- Repo scaffold created (this commit).

## In flight

- Nothing yet.

## Blocked

- Everything downstream of: (1) the actual competition/event facts in `COMPETITION.md`,
  (2) the thesis in `VISION.md`, (3) the stack decision in `ADR-0002`.

## Next intended step

TODO(Mulaydm10): fill `COMPETITION.md` and `VISION.md` first — nearly everything else in this
repo is inert until those two are real.

## Latest experiment

None yet. See `experiments/experiment_log.md`.

## Work claims

Claim a surface before starting on it; release it here the moment you yield. An agent that
finishes a unit of work updates this table (and the sections above) before stopping — see
`AGENTS.md` for the full protocol.

| Surface | Claimed by | Since | Status |
|---|---|---|---|
| _(none claimed)_ | — | — | — |
