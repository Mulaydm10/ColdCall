# Experiment log (schema-locked header, append-only rows)

Add a row per attempt worth remembering. Never edit or delete a past row — if an experiment is
revisited, add a new row referencing the old `EXP-####` id rather than editing it in place.

| ID | Date | What was tried | Result | Decision | Links |
|---|---|---|---|---|---|
| EXP-0001 | 2026-08-27 | Boot TrueForge standalone locally: `npx @truefoundry/trueforge` | **Works.** v0.1.4, SQLite at `~/Library/Application Support/trueforge/db/`, serves http://localhost:8790 (HTTP 200), API docs `/api/v1/docs`. Logs a **local sandbox fallback** available on darwin (bash + python3.14) alongside the documented Daytona provider. Auth disabled in standalone. | Use standalone `npx` route for the whole event; skip the docker-compose route (Docker not installed and not needed). Open `Q-0004` on whether the local fallback is enough for the sandbox judging beat. | `COMPETITION.md` → Required technology; `Q-0004` |
| EXP-0002 | 2026-08-27 | Verify every external data source in the plan by calling it, not by reading docs | **5 of 6 as claimed; 1 does not exist.** openFDA (`results[].storage_and_handling`), Open-Meteo forecast **and** archive, NWS, and OpenSky all work keyless. NWS 403s on an *empty* User-Agent — the header is load-bearing. OpenSky anonymous access still works in 2026 (~400 credits/day, `x-rate-limit-remaining` observed decrementing); OAuth2 is optional, not mandatory. **The VCC-CPLD dataset returns zero hits on Zenodo.** | Ship `scripts/verify_apis.sh` so this is re-checkable in 20s before a demo. Substitute the dataset — see `ADR-0003`. | `ADR-0003`, `Q-0007` |
| EXP-0003 | 2026-08-27 | Map TrueForge v0.1.4's real config surface from its live OpenAPI spec rather than the docs | **Three plan assumptions were wrong.** (1) No named-subagent registry exists — `dynamic_sub_agents` is the only mechanism, subagents share the root's tools and cannot nest. (2) No configurable local sandbox: `type` enum is `["daytona"]` alone. (3) `require_approval_for_tools` is per-entry in `mcp_servers[]`, not a top-level key. Also: `PUT` goes to the **collection** route (`/settings/skills`), not `/settings/skills/{name}` — the per-name route is read-only and 404s on a write, which the published docs get wrong. Compaction is flat `compaction_threshold_tokens`, not the nested `trigger` object in the docs. | Express the four specialists in the agent's instructions (`ADR-0004`). Treat Daytona as on the critical path. Script against the OpenAPI spec, never the docs. | `ADR-0004`, `Q-0004` |
| EXP-0004 | 2026-08-27 | Run the whole pipeline — stream the real Zenodo telemetry, weight by duration, compute MKT and a verdict | **Works end to end on real data.** A 2.9 MB range sample yields 3 818 usable readings across 6 devices; the largest leg is 1 786 readings over 39 days. Against a 2–8 °C biologic label every leg quarantines (the cargo is ambient, ~22–30 °C). Against a real 15–25 °C USP controlled-room-temperature label the same data gives a genuine spread: 2 review, 4 quarantine. | Frame the demo around a real ambient-storage product label from openFDA. The borderline `review` verdict is the interesting one to show. | `ADR-0003`, `Q-0007`, `EXP-0002` |
| EXP-0005 | 2026-08-27 | Configure the supplied Daytona API key in TrueForge | **Rejected with HTTP 422, "Daytona rejected the API key — check the credentials". The message is misleading: the key is valid.** Verified directly against Daytona: `GET /api/sandbox` 200, and `POST /api/sandbox` **successfully created a real sandbox** (deleted afterwards). But `POST /api/snapshots` returns **403 Forbidden**. Reading TrueForge's bundle: the PUT handler calls `provider.buildImage()`, and `isDaytonaAuthError` maps *any* Daytona 401/403 to that credentials message. buildImage needs to register a snapshot, so a key without snapshot permission fails looking exactly like a bad key. | The key needs **snapshot create permission**, not replacing. Regenerate it in Daytona with snapshot scope (or all permissions). Do not waste time re-checking the key itself — it demonstrably works for sandboxes. | `Q-0004`, `EXP-0001` |
| EXP-0006 | 2026-08-27 | First real run of `setup_trueforge.sh` against a populated `.env` | **Found a bug in our own script.** `.env.example` ships `STRIPE_SECRET_KEY=sk_test_` as a hint about the required form; the script tested only for non-empty, so the placeholder counted as configured and it registered a Stripe MCP connector that would have failed on first use — during a demo, most likely. Now rejected by an `is_placeholder` check (bare scheme prefixes, and anything under 12 characters). | Placeholders in an example file are not credentials. The already-registered bogus connector cannot be removed (TrueForge exposes no DELETE for MCP servers) but will be overwritten by the next PUT with a real token. | `Q-0008` |
| EXP-0007 | 2026-08-27 | Read TrueForge's bundle to find whether the Daytona 403 (`EXP-0005`) actually blocks anything | **It does not. Daytona is optional.** `resolveSandboxProvider()` returns the stored Daytona provider *if a record exists*, and otherwise — in standalone mode, where local support is available — constructs a **`LocalSandboxProvider`** (`type = "local"`, its own sandbox root, its own venv, code-mode transport). This machine's boot log confirms local support (darwin, bash, python3.14), and `GET /api/v1/capabilities` reports `sandbox.enabled: true` **and** `skill.enabled: true` with **zero** sandbox providers configured. This also corrects `EXP-0003`, which concluded from the *settings* schema (`type` enum `["daytona"]`) that no local sandbox existed — that enum governs what you may *configure*, not what the runtime *falls back to*. | **Do not configure Daytona.** The fallback only applies when no Daytona record is stored, so a configured-but-broken provider is strictly worse than none — the harness would use it and fail rather than fall back. `DAYTONA_API_KEY` is now commented out in `.env` and documented as opt-in. Unblocks `Q-0004`. | `EXP-0003`, `EXP-0005`, `Q-0004` |
| EXP-0008 | 2026-08-27 | Configure the working keys and prove the whole judged path end to end | **All green.** OpenAI: `gpt-5` registered (note the account has `gpt-5`/`gpt-5-mini`/`gpt-5-pro`, **not** the `gpt-5.6-sol` in TrueForge's catalog preset — the catalog is a suggestion, the key decides). Daytona: the replacement key carries `write:snapshots`, the provider reached `status: ready` after an image pull, and a session produced `sandbox: v1:daytona:…` returning **`Linux x86_64 3.13.15`** — a genuine remote microVM, not the macOS host. Network from inside is open (`github:200`, `git ls-remote` resolved `main`). Skills: the agent mounted `coldchain-sop` from GitHub *inside* the sandbox and quoted its governing rule back verbatim. | The scored path — real tool, sandboxed execution, skill loaded — is proven working, not asserted. Two gotchas worth keeping: `models[].properties` is **required** by the schema even though the catalog preset makes it look optional (omitting it is a 400), and the first Daytona `git ls-remote` failed with a transient `Connection reset by peer` that cleared on retry — build a retry into the demo rehearsal rather than assuming a clean first run. | `ADR-0005`, `EXP-0005`, `Q-0003`, `Q-0004` |
| EXP-0009 | 2026-08-27 | Why git skills fail on the **local** sandbox fallback (macOS) | TrueForge's own source explains it: `COMMAND_PATH_BY_PLATFORM.darwin` is commented *"prefer Homebrew ahead of `/usr/bin` shims (those need Xcode select paths that we intentionally do not allow-read)"*. This Mac's only git is the Xcode shim at `/usr/bin/git`, so the sandbox cannot run it. Not a network problem — `LOCAL_SANDBOX_ALLOWED_DOMAINS` already includes `github.com` and `*.githubusercontent.com`. Running the downloader by hand with a normal env succeeds, which is why it looked confusing. | Moot for the demo now that Daytona is live, but it means the **local fallback cannot mount git skills on this machine**. `brew install git` would fix it and make the fallback genuinely usable if Daytona fails mid-event. Deferred: it is a global install and the standing rule is nothing global. | `ADR-0005`, `EXP-0007` |
| EXP-0010 | 2026-08-27 | Run one full incident end to end against the live harness: excursion → strands → sandbox math → evidence bundle → approval gate → executed action with a receipt | **The whole loop works, and four bugs had to be fixed to get there — three of them mine, one in the build spec.** (1) A session body is `{"agent":{"spec":…}}`, not `{"agent":…}`; the spec's Appendix A.6 is wrong and the API just says "Invalid input at agent". (2) Compaction nests under `config.context_management.compaction`; a top-level `config.compaction` is rejected. (3) `turn.created` carries **`turn_id`** alongside its own event `id` — resuming an approval with the event id 404s "Turn not found", so the gate fires and the answer goes nowhere. (4) `ToolCallRef` carries only `{id, source_event_id}` — no tool name, no arguments — so a naive approval prompt shows the operator a bare id and asks them to approve it. **Proven:** the agent cloned the repo in a Daytona microVM, ran the deterministic module, and returned `quarantine_retest`, MKT **24.54 °C**, budget **64.35%** — byte-identical to the local run. It then attempted a real GitHub `create_branch`, **was held for approval**, and on deny reported the denial and stopped. On allow it created branch `incident/INC-VCC-118-A2231-20260827T090841Z` and committed the 5 984-byte deviation record as **`1c859fc`**. | Resolve every pending call back to its `model.message` before showing an approval prompt — an approval gate that displays `?` is the rubber stamp the SOP condemns. Pin the sandbox clone to an explicit ref. Keep the deviation report behind a real script, never an inline `python -c`. | `ADR-0004`, `EXP-0008`, `DEMO-0001` |
| EXP-0011 | 2026-08-27 | Why a live run failed with `path 'skills/coldchain-sop' not found in repository` while the script reported `ok` | **Our own script clobbered the caller's environment.** `setup_trueforge.sh` loaded `.env` with `set -a && . .env`, which *overrides* variables already exported. `.env` pins `COLDCALL_SKILL_REF=main`, so `COLDCALL_SKILL_REF=my-branch ./scripts/setup_trueforge.sh` silently registered the skill against `main` — and printed `ok`, because the PUT genuinely succeeded with the wrong ref. On `main` the skill path does not exist yet (`ADR-0006` deleted it; the restore is unmerged), hence the failure. Separately confirmed the **sandbox network is fine** (`github:200`, `git ls-remote` resolves, `pypi:200`), so the earlier `Connection reset by peer` was the transient cold-start flake `EXP-0008` already recorded — and it recurs per strand, because each strand gets its own sandbox. | Load `.env` with standard dotenv semantics: the environment wins over the file. Rehearse the demo at least twice — the skill fetch is a cold-start race and each strand rolls the dice separately. | `EXP-0008`, `EXP-0010` |
| EXP-0012 | 2026-08-27 | Chase down why the skill fetch went from an occasional flake to failing on every run | **It was never the network. Daytona's free-tier disk quota was full, and the error message points somewhere else entirely.** A bare probe turn returned the real cause: `Sandbox initialization failed: Total disk limit exceeded. Maximum allowed: 30GiB.` The account held **16 sandboxes / 48 GiB**. Every incident spawns one sandbox for the orchestrator and one per strand — five or six per run at ~3 GiB each — and `setup_trueforge.sh` was registering Daytona with `auto_delete_interval_in_minutes: 7200`, i.e. five days. What the harness surfaces when the ceiling is hit is `git ls-remote failed (exit 128) ... Recv failure: Connection reset by peer`, which is **indistinguishable from the transient cold-start race in `EXP-0011`** — so the obvious response (retry) fails again and sends you debugging networking. Worse, the agent proceeds **without its SOP skill** and produces a plausible-looking incident that never reaches an approval gate. | Reap before every session: `scripts/daytona_gc.sh` (dry-run by default; `--yes` to delete; never touches a running sandbox). Setup now registers `auto_archive: 15` / `auto_delete: 120` minutes instead of 60/7200. `replay/incident.py` retries once on a fresh session and **refuses to present a run whose skill never loaded** rather than letting a demo skip its own safety beat. Deleting the backlog needs a human — the DELETE was blocked by the local permission classifier as a destructive external action. | `EXP-0008`, `EXP-0011`, `DEMO-0001` |
