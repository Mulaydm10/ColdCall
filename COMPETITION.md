> **LOCKED governing file.** Do not edit in place. See `GOVERNANCE.md`.

# COMPETITION

This is the **single source of truth for event facts**. Every other file (README, DEMO.md,
notes/judging_alignment.md, STATE.md) links here rather than restating any of these fields —
if you catch a date, rubric weight, or rule duplicated elsewhere, delete the duplicate and
replace it with a link to the relevant heading below.

## Event

- **Name:** TODO(Mulaydm10)
- **Host / organizer:** TODO(Mulaydm10)
- **Event page / devpost link:** TODO(Mulaydm10)

## Deadline

- **Date:** TODO(Mulaydm10)
- **Time:** TODO(Mulaydm10)
- **Timezone:** TODO(Mulaydm10) — write it out explicitly (e.g. `CEST`, `UTC`); a bare time
  with no timezone has caused real missed submissions.

`STATE.md`'s "Time remaining" line is computed from this section — update both together.

## Submission format

TODO(Mulaydm10) — one or more of: repo link, demo video, live demo, devpost writeup. State
exactly what must be submitted and in what form; `submissions/README.md` stages the actual
deliverables once these are known.

## Judging rubric

TODO(Mulaydm10) — criteria and weights, verbatim from the organizers where possible.

| Criterion | Weight | Notes |
|---|---|---|
| TODO(Mulaydm10) | TODO | |

Once this table is real, mirror each row into `notes/judging_alignment.md` mapped to the
concrete artifact in this repo that satisfies it — that file is what stops us building
something judges never scored.

## Team roster

TODO(Mulaydm10) — full team, including anyone beyond the Main Agent (`Mulaydm10`, see
`GOVERNANCE.md` for locked-file authority).

## Hard rules

TODO(Mulaydm10) — what's allowed to be pre-built before the event window, licensing
constraints on dependencies, API-key / rate-limit constraints, any rule about AI-generated
code specifically. Leave blank items here rather than guessing; an assumed rule that turns out
wrong can disqualify a submission.
